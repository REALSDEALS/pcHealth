<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pcHealth.Pages.NinitePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="28,20,28,8" Spacing="4">
            <TextBlock Text="Get Ninite"
                       Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Downloads and launches the Ninite installer for common utility software."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="28,8,28,8" Spacing="12">

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="20,16">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE896;" FontSize="18" VerticalAlignment="Center"
                                      Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBlock Text="Ninite Installer" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="Installs Edge, Chrome, VLC, and 7-Zip silently with no toolbars or bloatware. The installer is downloaded fresh from ninite.com."/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="Download &amp; Run"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Command="{x:Bind ViewModel.DownloadCommand}"/>
                            <ProgressRing IsActive="{x:Bind ViewModel.IsDownloading, Mode=OneWay}"
                                          Width="22" Height="22" VerticalAlignment="Center"/>
                            <TextBlock Text="{x:Bind ViewModel.Status, Mode=OneWay}"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <InfoBar x:Name="ErrorBar" IsOpen="False" Severity="Error"
                                 Title="Download failed"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Padding="28,12,28,20" Spacing="8">
            <Button Content="Back" Click="BackBtn_Click"/>
        </StackPanel>
    </Grid>
</Page>
