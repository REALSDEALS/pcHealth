<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pcHealth.Pages.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer HorizontalScrollMode="Disabled"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="600" Margin="28,20,28,24" Spacing="16">

            <TextBlock Text="About pcHealth"
                       Style="{StaticResource TitleTextBlockStyle}"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8"
                    Padding="24,20">
                <StackPanel Spacing="8">
                    <Image x:Name="AppLogo"
                           Width="72" Height="72"
                           HorizontalAlignment="Left"
                           Margin="0,0,0,4"/>
                    <TextBlock Text="pcHealth"
                               Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <TextBlock x:Name="VersionText"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="Check the health of your Windows or Linux installation, drivers, updates, battery health and much more!"
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Links"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       Margin="0,8,0,0"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8"
                    Padding="20,14">
                <StackPanel Spacing="12">
                    <HyperlinkButton Content="View source code"
                                     NavigateUri="https://github.com/REALSDEALS/pcHealth"/>
                    <HyperlinkButton Content="Report an issue"
                                     NavigateUri="https://github.com/REALSDEALS/pcHealth/issues"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
