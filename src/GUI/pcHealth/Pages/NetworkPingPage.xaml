<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pcHealth.Pages.NetworkPingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="28,20,28,8" Spacing="4">
            <TextBlock Text="Short Ping Test"
                       Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Sends 4 ICMP packets to 8.8.8.8 (Google DNS) and displays latency."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Padding="28,0,28,12" Spacing="10">
            <Button Content="Run Test"
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.RunCommand}"/>
            <ProgressRing IsActive="{x:Bind ViewModel.IsRunning, Mode=OneWay}" Width="22" Height="22" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="28,0,28,8" Spacing="12">

                <!-- Results card -->
                <Border x:Name="ResultsCard" Visibility="Collapsed"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="20,16">
                    <StackPanel Spacing="10">
                        <TextBlock Text="Results" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <StackPanel x:Name="PingResultRows" Spacing="6"/>
                    </StackPanel>
                </Border>

                <!-- Summary card -->
                <Border x:Name="SummaryCard" Visibility="Collapsed"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="8" Padding="20,16">
                    <StackPanel Spacing="6">
                        <TextBlock Text="Summary" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <StackPanel x:Name="SummaryRows" Spacing="5"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Padding="28,12,28,20" Spacing="8">
            <Button Content="Back" Click="BackBtn_Click"/>
        </StackPanel>
    </Grid>
</Page>
